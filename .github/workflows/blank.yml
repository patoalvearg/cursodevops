name: workflow1

on:
 
  push:
    
jobs:
 
  construir:
    
    runs-on: ubuntu-latest

    steps:
    
      - name: Listar
        run: ls -al
      - name: Checkout
        uses: actions/checkout@v5.0.0
         
 
     